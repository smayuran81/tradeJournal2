# Next AG Grid Ticker (demo)

This is a small Next.js demo that uses AG Grid (community) to display currency pair tickers and a WYSIWYG editor to add notes for a selected ticker.

Quick start:

```bash
cd next-aggrid-ticker
npm install
npm run dev
```

Open http://localhost:3000
